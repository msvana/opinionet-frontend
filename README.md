# OpinioNet Frontend

## Run with Node

```sh
$ npm install
$ npm run dev
```

## Run with Docker

```sh
$ npm build
$ docker build -t opinionet_frontend .
$ docker run -p {host_port}:80 opinionet_frontend
```
